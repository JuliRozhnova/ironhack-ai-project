<img class="imgSelf" src="{{emotion.photo}}" alt="{{user.username}}">
<p>Your current mood: {{emotion.emotion}}</p>
<p>Your playlists:</p>

<div class="container">
  {{#each playlists}}
  <div>
    <div class="container-iframe">
      <iframe width="360" height="215" src="https://www.youtube.com/embed/videoseries?list={{id.playlistId}}"
        frameborder="0" allow="autoplay; encrypted-media" allowfullscreen></iframe>
    </div>
    <h3>{{snippet.title}}</h3>
    {{#if snippet.description}}
    <p>{{snippet.description}}</p>
    {{/if}}
    <a href="https://www.youtube.com/channel/{{snippet.channelId}}" alt='{{snippet.title}}' target="_blank">GO TO
      CHANNEL</a>
  </div>
  {{/each}}
</div>